# Copy to .env.local (never commit it). Vite reads it at build / dev-server start.

# Base URL of the backend API. Defaults to http://localhost:3000/api/v1.
# The backend's CORS_ORIGIN must include the origin this app is served from.
# VITE_API_BASE_URL=http://localhost:3000/api/v1
